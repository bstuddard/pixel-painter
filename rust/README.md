# Pixel Painter crate

See the [project README](../README.md) for setup, commands, releases, and licensing.

From this directory:

```sh
cargo run --locked -- --help
cargo run --locked -- --file examples/soccer-ball.json view
cargo test --locked
```
